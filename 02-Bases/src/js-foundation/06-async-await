const { httpClientPlugin } = require('../plugins/http-client.plugin');

const getPokemonByIdAsync = async (id) => {
  const url = 'https://pokeapi.co/api/v2/pokemon';

  const pokemon = await httpClientPlugin.get(`${url}/${id}`);

  return pokemon.name;
};

module.exports = { getPokemonByIdAsync };
